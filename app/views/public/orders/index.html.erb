<h5>注文履歴一覧</h5>

<table class="table table-bordered">
  <thead>
   <tr>
    <th>注文日</th>
    <th>配送先</th>
    <th>注文商品</th>
    <th>支払金額</th>
    <th>注文詳細</th>
   </tr>
  </thead>

   <% @orders.each do |orders| %>
    <tr>
     <td>><%= oeders.created_at %></td>
     <td>><%= orders.address %></td>
     <td>><%= @cart_item.item.name %></td>
     <td>><%= @total_all %></td>
     <td><%= link_to "表示する", order_path, :class => "btn btn-primary ms-3" %></td>
    </tr>
   <% end %>
 </table>